# Frontend port exposed by docker compose.
FRONTEND_PORT=3000

# Optional frontend container memory limit.
FRONTEND_MEM_LIMIT=768m

# Optional image repository used by deployment.
IMAGE_REPO=local

# Optional image tag used by deployment.
IMAGE_TAG=dev
